{
  "name": "webstore",
  "version": "1.0.0",
  "engines": {
    "node": "4.7.2"
  },
  "description": "",
  "main": "app.js",
  "scripts": {
    "setupDbRemote": "source .env && psql $DATABASE_URL < ./config/schema.sql $DATABASE_NAME",
    "setupDbLocal": "source local.env && psql $DATABASE_URL < ./config/schema.sql $DATABASE_NAME",
    "start": "nodemon app.js",
    "scratch": "nodemon scratch.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "bcrypt": "^1.0.2",
    "bluebird": "^3.5.0",
    "cookie-parser": "^1.4.3",
    "ejs": "^2.5.6",
    "express": "^4.15.3",
    "express-session": "^1.15.3",
    "nodemon": "^1.11.0",
    "pg-promise": "^6.3.4"
  }
}
